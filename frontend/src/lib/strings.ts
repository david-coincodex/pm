/**
 * DEPRECATED — strings are now managed via next-intl.
 * See frontend/messages/en.json and frontend/messages/de.json.
 * Use `useTranslations` (client components) or `getTranslations` (server components).
 */
